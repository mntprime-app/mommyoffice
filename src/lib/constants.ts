// ─── Site-wide defaults (safe to import anywhere — no server-only code) ───────

export const SETTING_DEFAULTS: Record<string, string> = {
  article_font_size:  '16',
  article_text_align: 'justify',
};
